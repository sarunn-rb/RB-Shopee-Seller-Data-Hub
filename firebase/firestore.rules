rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // V1 routes all reads and writes through authenticated Next.js server routes
    // using Firebase Admin. No business or credential collection is browser-readable.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
