import { PlaceholderPage } from "@/components/layout/placeholder-page";

export default function SettingsPage() {
  return (
    <PlaceholderPage
      title="Settings"
      description="Environment and organization settings will be exposed only through authenticated server-side operations."
    />
  );
}
