import { PlaceholderPage } from "@/components/layout/placeholder-page";

export default function MembersPage() {
  return (
    <PlaceholderPage
      title="Members"
      description="Invite-only Rabbit Bytes membership and admin/member roles are part of the Firebase Auth phase."
    />
  );
}
